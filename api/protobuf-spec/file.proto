syntax = "proto3";

package proto;

message Chunk {
    bytes Content = 1;
}

message UploadResponse {
    string message = 1;
}

service Uploader {
    rpc Upload(stream Chunk) returns (UploadResponse) {}
}
