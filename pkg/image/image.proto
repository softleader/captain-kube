syntax = "proto3";

message PullRequest {
    string host = 1;
    string repo = 2;
    string tag = 3;
}

message PullResponse {
    Result results = 1;
}

message Result {
    string tag = 1;
    string message = 2;
}

service Puller {
    rpc Pull (PullRequest) returns (PullResponse) {
    };
}