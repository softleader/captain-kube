syntax = "proto3";

package proto;

enum K8sVendor {
    Gcp = 0; // Google
    Icp = 1; // IBM
    OpenShift = 2; // RedHat
    Pks = 3; // Pivotal
}

message K8s {
    K8sVendor vendor = 1;
    string endpoint = 2;
}